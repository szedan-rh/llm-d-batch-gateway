# Scenario 1: No batch-gateway baseline
# No batch-gateway is deployed in this scenario.
# Batch-equivalent prompts are sent as regular interactive requests via a second guidellm instance.
# This file is intentionally empty — setup.sh skips batch-gateway install for scenario 1.
